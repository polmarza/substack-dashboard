# Your data and session — never commit these
data/
.profile/
# solo el de la raíz: el panel de la extensión sí va al repo
/dashboard.html

# Build artifacts
*.skill
node_modules/
store/*.zip
store/.stage/

.DS_Store
demo/
.cache/
